<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tree's Tech Blog</title>
  {{!-- need to add stylesheet or maybe bulma? :) --}}
</head>

<body>
  <div>
    <header>
      <h1>
        <a href="/">Tree's Tech Blog</a>
      </h1>
      <nav>
        {{#if logged_in}}
        <a href="/dashboard">Dashboard</a> |
        <button id="logout">Logout</button>
        {{else}}
        <a href="/login">Login</a>
        {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>